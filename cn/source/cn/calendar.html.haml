---
pageable: true
---

- content_for :title do
  = "Dotide官方博客"

.articles.small-12.large-9.columns.left
  - page_articles.each_with_index do |article, i|
    - if i > 0
      %hr/
    = partial '/cn/article', :locals => {:article => article, :digest => true}
  = partial '/cn/pagination', :locals => { |
      :paginate => paginate,               |
      :next_page => next_page,             |
      :prev_page => prev_page,             |
      :num_pages => num_pages,             |
      :page_number => page_number          |
    }                                      |
%aside.aside.small-12.large-3.columns.right
  %h3
    - case page_type
    - when 'day'
      = Date.new(year, month, day).strftime('%F')
    - when 'month'
      = Date.new(year, month, 1).strftime('%Y-%m')
    - when 'year'
      = year
